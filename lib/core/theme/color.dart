import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary (modern vibrant blue)
  static const Color primary = Color(0xFF2F6FE4);
  static const Color primaryLight = Color(0xFF5A9BFF);
  static const Color primaryDark = Color(0xFF1E4DB7);

  // Accent / Brand Feel
  static const Color accent = Color(0xFFFFF4CC); // Dairy cream tone
  static const Color secondary = Color(0xFF6EDC5F); // Fresh farm green

  // Gradient Colors (for splash / buttons / headers)
  static const Color gradientStart = Color(0xFF1C6DFF);
  static const Color gradientMiddle = Color(0xFF3A8DFF);
  static const Color gradientEnd = Color(0xFF6DB7FF);

  // Backgrounds (modern app background)
  static const Color background = Color(0xFFF4F7FF);
  static const Color cardcolor = Color(0xFFF1F7FB);
  static const Color scaffoldBackground = Color(0xFFF8FAFF);
  static const Color surface = Colors.white;

  // Text Colors (better contrast)
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF374151);
  static const Color textHint = Color(0xFF9CA3AF);
  static const Color textDisabled = Color(0xFFD1D5DB);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF06B6D4);

  // UI Elements
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFE5E7EB);
  static const Color cardBackground = Colors.white;

  // Shadows (modern soft shadow)
  static const Color shadow = Color(0x14000000);

  // Buttons
  static const Color buttonPrimary = primary;
  static const Color buttonSecondary = secondary;
  static const Color buttonDisabled = Color(0xFFE5E7EB);

  // Overlay
  static const Color overlay = Color(0x66000000);

  // Links
  static const Color link = primary;
}
